{
  "version": 1,
  "integration": {
    "validation": {
      "command": "cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --locked"
    },
    "signoff": {
      "mode": "none"
    }
  }
}
