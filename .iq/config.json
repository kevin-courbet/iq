{
  "version": 1,
  "integration": {
    "validation": {
      "command": "cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && mkdir -p target/tmp && TMPDIR=\"$PWD/target/tmp\" cargo test --locked -- --test-threads=1"
    },
    "signoff": {
      "mode": "none"
    }
  }
}
