# Schema 3 Fixture

`schema3-2a69e24.db` and its empty process-lock sidecar were generated by the IQ code at commit `2a69e24` through that release's `SqliteQueue::open` schema installer. Representative durable rows were then inserted under that schema's constraints. Current IQ code does not generate or modify this fixture.

IQ code at commit `db0dd7d` generated `schema5-db0dd7d.db` through `SqliteQueue::open`.
The migration test adds durable rows under the frozen schema-5 constraints. Current IQ code does not modify this fixture.

`rift_descriptor_probe.rs` confirms that an inherited Rift executable descriptor closes at the Rift exec boundary. It then delegates to the real Rift executable.
